# Server Configuration
PORT=3000

# MongoDB Configuration
# For local development (default):
# MONGODB_URI=mongodb://localhost:27017/doodle-app

# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/doodle-app?retryWrites=true&w=majority

# For Railway/Render with MongoDB add-on:
# MONGODB_URI will be automatically set by the platform
